#!/usr/bin/env sh
set -eu

if ! command -v uv >/dev/null 2>&1; then
  echo "uv is required to validate commit messages with Commitizen." >&2
  exit 1
fi

uv run --group dev cz check --commit-msg-file "$1"
